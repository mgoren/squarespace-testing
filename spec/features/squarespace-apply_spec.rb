require 'rails_helper'

feature 'leads created when application filled out at /apply' do
  scenario 'with valid info including gclid & sqf_source' do
    
    # expect(page).to have_content 'Signed in'
  end
end
